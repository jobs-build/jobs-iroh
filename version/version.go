// Package version carries the single release version of the jobs-iroh
// binaries. Bumped by release PRs; the mains surface it via --version.
package version

// Version is the current jobs-iroh release.
const Version = "0.31.1"
